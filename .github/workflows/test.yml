name: test

on:
  push:
    branches: 
        - main

jobs:
    build:
        runs-on: self-hosted
        steps:
            # - name: Check PowerShell
            #   run: $PSVersionTable
            
            - name: Check PowerShell
              uses: actions/checkout@v4